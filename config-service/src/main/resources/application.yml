server:
  port: 8888

spring:
  application:
    name: config-service
#  profiles:
#    active: native
  cloud:
    config:
      server:
#        native:
#          search-locations: file://${user.home}/rdiforest/workspace/configrepo
        git:
#          uri: file:///Users/rdijin/rdiforest/workspace/configrepo
          uri: https://github.com/Wordbe/spring-cloud-config
          default-label: main